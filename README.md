# ExploreCLI
CLI em Python para Previsão do Tempo, usando requests e WeatherAPI. Demonstra requisições dinâmicas, tratamento robusto de erros (JSONDecodeError/KeyError) e implementação de segurança de API Key via Variáveis de Ambiente (os.getenv). 
